Reads battery status (PB0, analogic input from ADC1 (channel 7)) translates it to milivolts and writes the data using the USART1 using printf().

PB0 is connected to a resistor divider (VBat - 47K ohms - PB0 - 20K - GND).

